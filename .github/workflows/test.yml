name: Test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  helper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run helper tests
        run: tests/test-helper.sh
